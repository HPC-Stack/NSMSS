spack:
  include:
  - ../../config/packages.yaml
  specs:
  - lammps +mpi +cuda  ^openmpi@4.1.7a1+cuda %nvhpc

  view: false

  concretizer:
    unify: when_possible

  mirrors:
    rudra: /scratch/parikshit/spack-cache


